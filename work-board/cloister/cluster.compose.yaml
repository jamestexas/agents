# AUTO-GENERATED by scripts/emit-compose.mjs. Do NOT edit by hand.
# Regenerate via Cloister's cluster TOML pipeline after editing cluster.toml.
#
# Cluster: work-board-smoke v0.1.0
# 2 bundle(s), 0 wire(s)

name: work-board-smoke

services:
  canonical-hours-fixture:
    image: canonical-hours-fixture:work-board-smoke
    pull_policy: never
    container_name: cloister-canonical-hours-fixture
    labels:
      - "cloister.bundle=canonical-hours-fixture"
      - "cloister.tier=cluster"
      - "cloister.description=canonical-hours-fixture:work-board-smoke — Deterministic Canonical Hours-compatible HTTP source"
    ports:
      - "8790:8790"
    volumes:
      - cloister-uds:/run/cloister-uds

  work-board:
    image: work-board:smoke
    pull_policy: never
    container_name: cloister-work-board
    labels:
      - "cloister.bundle=work-board"
      - "cloister.tier=cluster"
      - "cloister.description=work-board:smoke — Staged workerd UI and BoardSource adapter"
    ports:
      - "8791:8791"
    volumes:
      - cloister-uds:/run/cloister-uds
    environment:
      - "CANONICAL_HOURS_URL=http://canonical-hours-fixture:8790"
      - "WORK_BOARD_FIXTURE=false"
      - "WORK_BOARD_REFRESH_MODE=source-authorized"

volumes:
  cloister-uds:
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: "mode=1777"
  cloister-do:
    driver: local
    # SQLite DO storage — survives container restarts; backup target
